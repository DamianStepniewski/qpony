{% extends 'base.html.twig' %}

{% block body %}
    <table class="table">
        <thead>
        <tr>
            <th>Input</th><th>Output</th>
        </tr>
        </thead>
        <tbody>
        {% for key, value in data %}
            <tr>
                <td>{{ key }}</td>
                <td>{{ value }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="row justify-content-center">
        <a href=""><button type="button" class="btn-primary btn">Wstecz</button></a>
    </div>
{% endblock %}
